# String
var string_type = "Hello World"
var whitespaces = " "
var emptystring = ""
var fox_strings = "The quick brown fox jumps over the lazy dog"

# Raw string literals and Multiline string literals
var raw_string_literal = r"Raw string literals"
var multiline_string = """
This is a Multiline String, of string type, it can have quote characters inside,
like ', ", \n , \r, \c, \x00, \t, Emoji, etc without producing any errors. :)
"""

# Char
var char_type = 'a'
var whitespace = ' '
var another = '9'
